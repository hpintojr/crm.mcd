*** Begin Patch
*** Update File: prisma/schema.prisma
@@
 model Lead {
@@
   normalizedPhone   String?
   website           String?
+  businessAddress   String?
   industry          String?
@@
   timezone          String?
+  googleRating      Decimal?      @db.Decimal(2, 1)
+  googleRatingObservedAt DateTime?
+  googleMapsUrl     String?
@@
 model LeadImportBatch {
@@
   failureReason        String?
   rows                 LeadImportRow[]
+  ownerAcquisitionProvenance OwnerLeadAcquisitionProvenance?
@@
 }
+
+model OwnerLeadAcquisitionProvenance {
+  id                   String          @id @default(cuid())
+  leadImportBatchId    String          @unique
+  batch                LeadImportBatch @relation(fields: [leadImportBatchId], references: [id], onDelete: Cascade)
+  sourceCode           String
+  acquisitionReference String
+  providerName         String?
+  createdAt            DateTime        @default(now())
+  updatedAt            DateTime        @updatedAt
+}
*** End Patch
