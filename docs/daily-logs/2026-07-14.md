# 2026-07-14 — Agent activation gates and housekeeping

## Built

- Added internal activation gate evidence fields to `Agent` (`w9VerifiedAt/-ById`, `profileCompletedAt/-ById`, `trainingCompletedAt/-ById`) with a staged additive migration (`20260714120000_add_agent_activation_gates`) — not applied to production.
- Added the pure derived-state policy `src/lib/agent-activation-policy.ts`: `APPLICANT_IN_REVIEW → DOCUMENTS_IN_PROGRESS → DOCUMENTS_COMPLETE → W9_VERIFIED → PROFILE_COMPLETE → TRAINING_COMPLETE → ACTIVE_PARTNER`, with `mayIssueActivation` true only when documents, countersignature, and all three internal gates hold.
- Gated `/api/ghl/documents` provisioning on the policy; blocked responses now report `activationState` and `missingInternalGates` without provisioning.
- Added an "Internal activation gates" recording section to `/admin/agents/:id/onboarding` with required notes and `AGENT_ACTIVATION_GATE_RECORDED`/`_CLEARED` audit entries.
- Added `scripts/check-agent-activation-policy.ts`, `npm run check:agent-activation-policy`, and the Agent Activation Policy CI workflow.

## Decisions

- Owner decision: build the full slice (schema + policy + webhook gate + admin UI); grandfather agents provisioned before the gates existed.
- Owner decision: merged PR #139 (Stripe Connect readiness foundation) after review; CI green, code inert, all payment flags remain disabled.
- Housekeeping: closed issue #10 (superseded — Leads fully live with 18/18 acceptance PASS) and issue #3 (not planned in that form — the `neon-safety`/`production` GitHub Environments were never created; releases follow the Neon safety-branch + explicit-authorization discipline).

## Gated / next controlled step

- Do not merge the activation-gates branch until the additive migration is rehearsed on a disposable Neon branch and applied to production under explicit owner authorization (Prisma selects all columns; deploying first would break Agent reads).
- After merge, record the three gates for any in-flight applicants before their documents complete, or they will correctly hold at `DOCUMENTS_COMPLETE`.
