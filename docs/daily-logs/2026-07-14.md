# 2026-07-14 — Fail-closed approval deploy + onboarding packet coordinator (staged)

## Built

- Merged and deployed PR #140 (`codex/onboarding-handoff-failsafe-20260713`): applicant
  approval now fails closed if the GHL contact is missing or the `agent-approved` tag
  write fails. Confirmed live via the Vercel production deployment for the merge commit.
- Staged the Option B onboarding packet coordinator (four documents via GHL Send
  Template, one composed CRM email with all four secure links, sent only if all four
  succeed): `src/lib/ghl-documents.ts`, `src/lib/emails/onboarding-packet-email.ts`,
  `src/lib/onboarding-packet-dispatch-policy.ts`, `src/lib/onboarding-packet-coordinator.ts`.
- Added `scripts/check-onboarding-packet-coordinator.ts`: synthetic-data proof of the
  email composition and fail-closed dispatch policy (all-success, single-failure,
  all-failure, exactly-four-links contract, HTML-escaping). No live GHL or SMTP calls.

## Verified

- The failsafe fix was already code-reviewed against `src/lib/ghl.ts`'s real
  `addContactTag` implementation before merge; confirmed the tag-write result gates
  applicant status in production, not just in a stub path.
- PR #2 in `hpintojr/My-Workspace` (2026-07-14 CRM + GHL Implementation Handoff and status
  docs) reviewed as documentation-only — no business terms, pricing, or commission
  changes — and merged to `main`.
- Stripe Connect PR #139 re-confirmed readiness-only by reading all 10 changed files; not
  touched today.
- The onboarding packet coordinator's composition and dispatch-policy logic is proven
  with fabricated document ids, signing URLs, and recipient names — see
  `docs/ONBOARDING_PACKET_COORDINATOR.md`.

## Gated / not done

- The onboarding packet coordinator is **not wired into any live flow** and is gated
  behind `ONBOARDING_PACKET_COORDINATOR_ENABLED` (default `false`). Its GHL Send Template
  response field mapping (`src/lib/ghl-documents.ts`) is unverified — no live GHL
  credential was available in this session to confirm it.
- Before enabling: verify the live field mapping, disable/unpublish the native "Agent
  Onboarding Documents" GHL workflow's four Send Document actions to avoid double-sending
  documents, set the four `GHL_TEMPLATE_ID_*` values, wire `dispatchOnboardingPacket()`
  into an admin action, then run one owner-authorized synthetic end-to-end approval.
- No real applicant was approved, no live document was sent, and no live email was
  composed or delivered while building or proving this.
