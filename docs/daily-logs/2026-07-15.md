# 2026-07-15 — Activation gates release and structured error tracking

## Released

- Rehearsed the `add_agent_activation_gates` migration on disposable Neon branch `activation-gates-rehearsal-20260715` (all catalog and record/clear lifecycle checks PASS; evidence on PR #142).
- With explicit owner authorization, applied the same additive DDL to Neon production and verified the catalog; no rows modified.
- Merged PR #142 (internal agent activation gates). Grandfathering confirmed: existing provisioned agents read NULL evidence and keep access.

## Built

- Server-only structured error tracking via `@sentry/nextjs`: `instrumentation.ts` (Node runtime, gated on `SENTRY_DSN`, no tracing, no default PII) with `onRequestError` capture.
- `src/lib/error-tracking.ts` + `logIntegrationError` forwarding: source, message, and reference id only; webhook bodies and stored JSON stay in the `IntegrationError` table.
- `scripts/check-error-tracking-boundary.ts`, `npm run check:error-tracking-boundary`, and the Error Tracking Boundary CI workflow.

## Verified

- No DSN is committed anywhere; tracking is fully disabled until `SENTRY_DSN` is configured in Vercel.
- No client-side instrumentation or `NEXT_PUBLIC_` Sentry surface exists (guard-enforced).

## Gated / next controlled step

- Configure `SENTRY_DSN` (and optional `SENTRY_ENVIRONMENT`) in Vercel to activate tracking; no code change required.
- Record W-9/profile/training gates for any in-flight applicants before their documents complete.

## Also built (same day)

- Service cadence cron: `/api/cron/servicing/cadence` (daily 12:30 UTC via Vercel Cron) running `runServiceCadenceSweep` — opens `SERVICE_CADENCE` Service Cases per `requiredCadence` package rules, anchored on launch/health-confirmation/last-cadence-case, deduped on open cases, audited as SYSTEM. 404s while `SERVICING_ENABLED=false`.
- Pure schedule module `src/lib/service-cadence-schedule.ts` with guard coverage (`check:service-cadence-cron` + Service Cadence Policy workflow).
- Staged additive enum migration `20260715120000_add_service_cadence_trigger` (`ALTER TYPE ... ADD VALUE 'SERVICE_CADENCE'`) — **not applied**; apply with owner authorization before the Servicing acceptance window. Reads compare trigger as text, so merged code is safe pre-migration.
- Deleted `src/lib/service-cadence.ts` (unused draft whose thresholds conflicted with the roadmap rules); `src/lib/service-rules.ts` is the single cadence source.
