import Link from "next/link";
import { ADMIN_ROLES, requireRole } from "@/lib/authz";
import { db } from "@/lib/db";

export const dynamic = "force-dynamic";

const acceptanceActions = ["LEAD_ACCEPTANCE_RECORDED", "SERVICING_ACCEPTANCE_RECORDED", "COMMISSION_ACCEPTANCE_RECORDED"];

export default async function AuditPage() {
  await requireRole(ADMIN_ROLES);
  const [entries, rolloutEvidence] = await Promise.all([
    db.auditLog.findMany({ orderBy: { createdAt: "desc" }, take: 250 }),
    db.auditLog.findMany({ where: { actionType: { in: acceptanceActions } }, orderBy: { createdAt: "desc" }, take: 50 }),
  ]);
  return <main className="mx-auto min-h-screen max-w-7xl px-6 py-12"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-medium uppercase tracking-widest text-brand-400">Mercury Call Desk</p><h1 className="mt-2 text-3xl font-semibold text-white">Audit history</h1><p className="mt-2 text-gray-400">Recent sensitive and operational actions recorded by the Mini CRM.</p></div><Link className="rounded-lg border border-ink-700 px-3 py-2 text-sm text-gray-200" href="/admin/readiness">Readiness board</Link></div><section className="mt-8 overflow-hidden rounded-2xl border border-ink-700 bg-ink-900"><div className="flex flex-wrap items-start justify-between gap-4 border-b border-ink-700 px-6 py-4"><div><h2 className="font-semibold text-white">Rollout acceptance evidence</h2><p className="mt-1 text-sm text-gray-400">Recorded Pass, Fail, and Deferred results from controlled Lead, Servicing, and Commission testing.</p></div><div className="flex gap-2"><Link className="rounded-lg border border-brand-500 px-3 py-2 text-sm text-brand-200" href="/admin/leads/testing">Leads</Link><Link className="rounded-lg border border-brand-500 px-3 py-2 text-sm text-brand-200" href="/admin/servicing/testing">Servicing</Link><Link className="rounded-lg border border-brand-500 px-3 py-2 text-sm text-brand-200" href="/admin/commissions/testing">Commissions</Link></div></div>{rolloutEvidence.length === 0 ? <p className="px-6 py-8 text-sm text-gray-400">No acceptance results have been recorded yet.</p> : <div className="divide-y divide-ink-700">{rolloutEvidence.map((entry) => <article className="px-6 py-4" key={entry.id}><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-medium text-white">{entry.actionType.replaceAll("_", " ")}</p><p className="mt-1 text-sm text-gray-400">{entry.entityType}{entry.entityId ? ` · ${entry.entityId}` : ""}</p>{entry.reason && <p className="mt-2 text-sm text-gray-300">{entry.reason}</p>}</div><div className="text-right text-xs text-gray-500"><p>{entry.actorRole || "System"}</p><p className="mt-1">{entry.createdAt.toLocaleString("en-US", { dateStyle: "medium", timeStyle: "short", timeZone: "America/Los_Angeles" })}</p></div></div></article>)}</div>}</section><section className="mt-8 overflow-hidden rounded-2xl border border-ink-700 bg-ink-900">{entries.length === 0 ? <p className="px-6 py-10 text-sm text-gray-400">No audit records yet.</p> : <div className="divide-y divide-ink-700">{entries.map((entry) => <article className="px-6 py-4" key={entry.id}><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-medium text-white">{entry.actionType}</p><p className="mt-1 text-sm text-gray-400">{entry.entityType}{entry.entityId ? ` · ${entry.entityId}` : ""}</p>{entry.reason && <p className="mt-2 text-sm text-gray-300">Reason: {entry.reason}</p>}</div><div className="text-right text-xs text-gray-500"><p>{entry.actorRole || "System"}</p><p className="mt-1">{entry.createdAt.toLocaleString("en-US", { dateStyle: "medium", timeStyle: "short", timeZone: "America/Los_Angeles" })}</p></div></div></article>)}</div>}</section></main>;
}
