name: Lead Import Response Contract

on:
  pull_request:
    paths:
      - "src/lib/lead-import-response-contract.ts"
      - "scripts/check-lead-import-response-contract.ts"
      - ".github/workflows/lead-import-response-contract.yml"
  push:
    branches:
      - "rebuild/lead-import-response-contract-v2"

permissions:
  contents: read

jobs:
  response-contract-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm install --ignore-scripts --no-audit --no-fund
      - run: npx tsx scripts/check-lead-import-response-contract.ts
