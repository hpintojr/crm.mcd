name: Error Tracking Boundary

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  error-tracking-boundary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --ignore-scripts
      - run: npx tsx scripts/check-error-tracking-boundary.ts
